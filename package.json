{
  "name": "ethara",
  "version": "1.0.0",
  "private": true,
  "description": "Ethara collaborative task management — root orchestrator for Railway single-service deploy.",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "install:backend": "npm install --prefix backend",
    "install:frontend": "npm install --prefix frontend --include=dev",
    "build:frontend": "npm run build --prefix frontend",
    "build": "npm run install:backend && npm run install:frontend && npm run build:frontend",
    "postinstall": "npm run install:backend",
    "start": "npm run install:backend && node backend/src/server.js"
  }
}
